---
- name: Create frontend env file
  template:
    src: frontend.env.j2
    dest: "{{ app_dir }}/frontend/.env"
    owner: ubuntu
    group: ubuntu
    
- name: Install frontend packages
  npm:
    path: "{{ app_dir }}/frontend"
    state: present
  become: true
  become_user: ubuntu

- name: Build frontend
  shell: npm run build
  args:
    chdir: "{{ app_dir }}/frontend"
    creates: "{{ app_dir }}/frontend/build/index.html"
  become: true
  become_user: ubuntu